-- Audit Triggers Migration for banking module
-- Generated by metaphor-schema
--
-- This migration adds automatic timestamp management triggers
-- to all tables with audit metadata fields.
--
-- Triggers automatically set:
--   - metadata->'created_at' on INSERT
--   - metadata->'updated_at' on INSERT and UPDATE

-- ==============================================================================
-- Table: Bank (banking.banks)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.banks_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS banks_insert_audit ON banking.banks;
CREATE TRIGGER banks_insert_audit BEFORE INSERT ON banking.banks
    FOR EACH ROW EXECUTE FUNCTION banking.banks_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS banks_update_audit ON banking.banks;
CREATE TRIGGER banks_update_audit BEFORE UPDATE ON banking.banks
    FOR EACH ROW EXECUTE FUNCTION banking.banks_audit_timestamp();

-- ==============================================================================
-- Table: BankAccount (banking.bank_accounts)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.bank_accounts_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS bank_accounts_insert_audit ON banking.bank_accounts;
CREATE TRIGGER bank_accounts_insert_audit BEFORE INSERT ON banking.bank_accounts
    FOR EACH ROW EXECUTE FUNCTION banking.bank_accounts_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS bank_accounts_update_audit ON banking.bank_accounts;
CREATE TRIGGER bank_accounts_update_audit BEFORE UPDATE ON banking.bank_accounts
    FOR EACH ROW EXECUTE FUNCTION banking.bank_accounts_audit_timestamp();

-- ==============================================================================
-- Table: BankClearance (banking.bank_clearances)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.bank_clearances_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS bank_clearances_insert_audit ON banking.bank_clearances;
CREATE TRIGGER bank_clearances_insert_audit BEFORE INSERT ON banking.bank_clearances
    FOR EACH ROW EXECUTE FUNCTION banking.bank_clearances_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS bank_clearances_update_audit ON banking.bank_clearances;
CREATE TRIGGER bank_clearances_update_audit BEFORE UPDATE ON banking.bank_clearances
    FOR EACH ROW EXECUTE FUNCTION banking.bank_clearances_audit_timestamp();

-- ==============================================================================
-- Table: BankReconciliation (banking.bank_reconciliations)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.bank_reconciliations_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS bank_reconciliations_insert_audit ON banking.bank_reconciliations;
CREATE TRIGGER bank_reconciliations_insert_audit BEFORE INSERT ON banking.bank_reconciliations
    FOR EACH ROW EXECUTE FUNCTION banking.bank_reconciliations_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS bank_reconciliations_update_audit ON banking.bank_reconciliations;
CREATE TRIGGER bank_reconciliations_update_audit BEFORE UPDATE ON banking.bank_reconciliations
    FOR EACH ROW EXECUTE FUNCTION banking.bank_reconciliations_audit_timestamp();

-- ==============================================================================
-- Table: BankStatementImport (banking.bank_statement_imports)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.bank_statement_imports_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS bank_statement_imports_insert_audit ON banking.bank_statement_imports;
CREATE TRIGGER bank_statement_imports_insert_audit BEFORE INSERT ON banking.bank_statement_imports
    FOR EACH ROW EXECUTE FUNCTION banking.bank_statement_imports_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS bank_statement_imports_update_audit ON banking.bank_statement_imports;
CREATE TRIGGER bank_statement_imports_update_audit BEFORE UPDATE ON banking.bank_statement_imports
    FOR EACH ROW EXECUTE FUNCTION banking.bank_statement_imports_audit_timestamp();

-- ==============================================================================
-- Table: BankTransaction (banking.bank_transactions)
-- ==============================================================================

-- Function to set metadata timestamps
CREATE OR REPLACE FUNCTION banking.bank_transactions_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS bank_transactions_insert_audit ON banking.bank_transactions;
CREATE TRIGGER bank_transactions_insert_audit BEFORE INSERT ON banking.bank_transactions
    FOR EACH ROW EXECUTE FUNCTION banking.bank_transactions_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS bank_transactions_update_audit ON banking.bank_transactions;
CREATE TRIGGER bank_transactions_update_audit BEFORE UPDATE ON banking.bank_transactions
    FOR EACH ROW EXECUTE FUNCTION banking.bank_transactions_audit_timestamp();

