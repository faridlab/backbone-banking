//! Repository trait for BankReconciliation aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BankReconciliation aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{BankReconciliation, ReconStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BankReconciliationPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl BankReconciliationPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct BankReconciliationPaginatedResult {
    pub data: Vec<BankReconciliation>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BankReconciliationFilter {
    pub company_id: Option<Uuid>,
    pub bank_account_id: Option<Uuid>,
    pub status: Option<ReconStatus>,
}

impl BankReconciliationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.bank_account_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for BankReconciliation aggregate
///
/// This trait defines the persistence contract for BankReconciliation entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BankReconciliationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new bank_reconciliation entity
    async fn save(&self, entity: &BankReconciliation) -> Result<BankReconciliation>;

    /// Find bank_reconciliation by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BankReconciliation>>;

    /// Find all bank_reconciliation entities
    async fn find_all(&self) -> Result<Vec<BankReconciliation>>;

    /// Update bank_reconciliation by ID
    async fn update(&self, id: &str, entity: &BankReconciliation) -> Result<Option<BankReconciliation>>;

    /// Delete bank_reconciliation by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List bank_reconciliation with pagination
    async fn list(&self, params: BankReconciliationPaginationParams) -> Result<BankReconciliationPaginatedResult>;

    /// List bank_reconciliation with pagination and filters
    async fn list_with_filters(&self, params: BankReconciliationPaginationParams, filters: BankReconciliationFilter) -> Result<BankReconciliationPaginatedResult>;

    /// Count all bank_reconciliation entities
    async fn count(&self) -> Result<u64>;

    /// Count bank_reconciliation entities matching filters
    async fn count_with_filters(&self, filters: BankReconciliationFilter) -> Result<u64>;

    /// Check if bank_reconciliation exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete bank_reconciliation by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted bank_reconciliation by ID
    async fn restore(&self, id: &str) -> Result<Option<BankReconciliation>>;

    /// List soft-deleted bank_reconciliation entities
    async fn list_deleted(&self, params: BankReconciliationPaginationParams) -> Result<BankReconciliationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save bank_reconciliation entities
    async fn bulk_save(&self, entities: &[BankReconciliation]) -> Result<Vec<BankReconciliation>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
