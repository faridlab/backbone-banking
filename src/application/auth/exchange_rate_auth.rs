//! Auth configuration for ExchangeRate handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ExchangeRate>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ExchangeRateService;
use crate::domain::entity::ExchangeRate;

/// Permission string constants for ExchangeRate operations.
pub mod permissions {
    pub const LIST:        &str = "exchange_rate:list";
    pub const READ:        &str = "exchange_rate:read";
    pub const CREATE:      &str = "exchange_rate:create";
    pub const UPDATE:      &str = "exchange_rate:update";
    pub const DELETE:      &str = "exchange_rate:delete";
    pub const RESTORE:     &str = "exchange_rate:restore";
    pub const TRASH:       &str = "exchange_rate:trash";
    pub const EMPTY_TRASH: &str = "exchange_rate:empty_trash";
    pub const BULK_CREATE: &str = "exchange_rate:bulk_create";
    pub const UPSERT:      &str = "exchange_rate:upsert";
}

/// Resource policy for ExchangeRate — maps CRUD actions to permission strings.
pub struct ExchangeRatePolicy;

#[async_trait]
impl ResourcePolicy<ExchangeRate> for ExchangeRatePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ExchangeRate,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for ExchangeRate handlers.
///
/// Wraps `ExchangeRateService` and checks `ExchangeRatePolicy` before delegating.
pub type ExchangeRateGuard = PermissionGuard<ExchangeRate>;

/// Service-integrated permission guard for ExchangeRate (for use in generated handlers).
pub type ExchangeRateServiceGuard = ServicePermissionGuard<ExchangeRate, ExchangeRateService, ExchangeRatePolicy>;

// <<< CUSTOM
// END CUSTOM
