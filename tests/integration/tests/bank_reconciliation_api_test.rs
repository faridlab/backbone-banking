//! BankReconciliation API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the BankReconciliation CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// BANKRECONCILIATION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for BankReconciliation
pub struct BankReconciliationTestData;

impl TestDataGenerator for BankReconciliationTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "from_date": Utc::now().format("%Y-%m-%d").to_string(),
            "to_date": Utc::now().format("%Y-%m-%d").to_string(),
            "statement_closing_balance": 0,
            "ledger_balance": 0,
            "computed_difference": 0,
            "unreconciled_count": 1,
            "status": "open",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "from_date": Utc::now().format("%Y-%m-%d").to_string(),
            "to_date": Utc::now().format("%Y-%m-%d").to_string(),
            "statement_closing_balance": 0,
            "ledger_balance": 0,
            "computed_difference": 0,
            "unreconciled_count": 1,
            "status": "open",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// BANKRECONCILIATION API TEST
// ============================================================================

/// BankReconciliation API test suite
pub struct BankReconciliationApiTest {
    inner: GenericCrudTest<BankReconciliationTestData>,
}

impl BankReconciliationApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/bank_reconciliations", "BankReconciliation");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, BankReconciliationTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for BankReconciliationApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_bank_reconciliation_crud() {
        let mut test = BankReconciliationApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add BankReconciliation specific tests here
// <<< CUSTOM TESTS END >>>
