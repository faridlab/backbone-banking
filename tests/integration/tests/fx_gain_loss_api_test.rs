//! FxGainLoss API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the FxGainLoss CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// FXGAINLOSS TEST DATA GENERATOR
// ============================================================================

/// Test data generator for FxGainLoss
pub struct FxGainLossTestData;

impl TestDataGenerator for FxGainLossTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "bank_clearance_id": null,
            "matched_source_id": Uuid::new_v4().to_string(),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "original_rate": 0,
            "realised_rate": 0,
            "base_amount_delta": 0,
            "direction": "gain",
            "fx_account_id": Uuid::new_v4().to_string(),
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "bank_clearance_id": null,
            "matched_source_id": Uuid::new_v4().to_string(),
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "original_rate": 0,
            "realised_rate": 0,
            "base_amount_delta": 0,
            "direction": "gain",
            "fx_account_id": Uuid::new_v4().to_string(),
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// FXGAINLOSS API TEST
// ============================================================================

/// FxGainLoss API test suite
pub struct FxGainLossApiTest {
    inner: GenericCrudTest<FxGainLossTestData>,
}

impl FxGainLossApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/fx_gain_losses", "FxGainLoss");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, FxGainLossTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for FxGainLossApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_fx_gain_loss_crud() {
        let mut test = FxGainLossApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add FxGainLoss specific tests here
// <<< CUSTOM TESTS END >>>
