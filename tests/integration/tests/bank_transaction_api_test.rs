//! BankTransaction API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the BankTransaction CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// BANKTRANSACTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for BankTransaction
pub struct BankTransactionTestData;

impl TestDataGenerator for BankTransactionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "import_id": Uuid::new_v4().to_string(),
            "txn_date": Utc::now().format("%Y-%m-%d").to_string(),
            "value_date": null,
            "description": null,
            "reference_no": null,
            "deposit": 0,
            "withdrawal": 0,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "status": "unreconciled",
            "allocated_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "bank_account_id": Uuid::new_v4().to_string(),
            "import_id": Uuid::new_v4().to_string(),
            "txn_date": Utc::now().format("%Y-%m-%d").to_string(),
            "value_date": null,
            "description": null,
            "reference_no": null,
            "deposit": 0,
            "withdrawal": 0,
            "currency": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "status": "unreconciled",
            "allocated_amount": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/bank_statement_imports", &super::bank_statement_import_api_test::BankStatementImportTestData).await {
            deps.push(("import_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// BANKTRANSACTION API TEST
// ============================================================================

/// BankTransaction API test suite
pub struct BankTransactionApiTest {
    inner: GenericCrudTest<BankTransactionTestData>,
}

impl BankTransactionApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/bank_transactions", "BankTransaction");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, BankTransactionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for BankTransactionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_bank_transaction_crud() {
        let mut test = BankTransactionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add BankTransaction specific tests here
// <<< CUSTOM TESTS END >>>
